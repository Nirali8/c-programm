#include <stdio.h>

int main()
{
    int l,w;
    printf("length and width of rectangle :");
    scanf("%d %d",&l,&w);
    printf("Area of rectangle is = %d\n",l*w);
    printf("Perimeter of rectangle is = %d\n",2*(l+w));

    return 0;
}
