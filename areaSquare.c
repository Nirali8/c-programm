#include <stdio.h>

int main()
{
    int l;
    printf("length square :");
    scanf("%d",&l);
    printf("Area of square is = %d\n",l*l);
    printf("Perimeter of square is = %d\n",4*l);

    return 0;
}
